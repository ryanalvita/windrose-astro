---
// Props: tableData, binCount
const { tableData, binCount } = Astro.props;
---

<div>
    <h2>Chart</h2>
    <div class="bg-white border-2 border-gray-300 rounded-lg p-4 shadow-sm">
        <div id="windrose-plot" style="height: 600px; width: 100%"></div>
    </div>
</div>
<script>
    import Plotly from "plotly.js-dist-min";
    if (tableData && tableData.length > 0) {
        // ...existing renderWindrose logic...
        const layout = {
            // ...existing layout properties...
            legend: {
                x: 0.5,
                y: -0.2,
                xanchor: "center",
                orientation: "h",
            },
            // ...existing code...
        };
        // ...existing code...
    }
</script>
